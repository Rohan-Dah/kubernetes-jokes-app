{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/jokes-api/package.json",
                "localRoot": "${workspaceFolder}/jokes-api"
            }
        }
    ]
}